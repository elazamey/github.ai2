# حكم بوابة REAL-GITHUB-E2E — 2026-09-05

```text
Local MVP        = VERIFIED
REAL-GITHUB-E2E  = BLOCKED  (سبب واحد محدد: صلاحيات GitHub — لا يوجد خلل في النظام)
```

## ملخص التنفيذ

نفّذ اليوم (محليًا + على GitHub الحقيقي) كل ما يمكن تنفيذه ضمن صلاحيات بيئة التنفيذ الحالية:

| # | البند | النتيجة | الدليل |
|---|-------|---------|--------|
| 0 | إعادة تأكيد Local MVP (`npm test`) | **PASS** (11 فحصًا) | `evidence/.../smoke-test.log` + `smoke-ledger-20260905.jsonl` |
| 1 | Required Check `e2e` على `main` | **BLOCKED** | لا يمكن قراءتها **ولا** كتابتها: `GET/PUT .../branches/main/protection` → 403، و`POST .../rulesets` → 403 (صلاحيات `Administration` غير مسموحة للتوكن) |
| 2 | `npm run test:real` محليًا | **شُغّل** — انتهى بـ `BLOCKED` (فشل مغلق عند قراءة حماية الفرع) | `evidence/.../harness-output.log` |
| 3 | Positive E2E (Squash Merge) | **BLOCKED** — الوكيل رفض الدمج رغم CI الأخضر الكامل لأنه لا يستطيع التحقق من حماية الفرع (403) | `evidence/.../driver-results.json` (A2) + `ledger.json` |
| 4a | CI-failure | **PASS (مرفوض كما يجب)** — 409 «فشل 1 من فحوصات CI؛ تم منع الدمج» | PR #2، `ledger.json` (EXECUTION BLOCKED) |
| 4b | Unprotected branch | **BLOCKED (الحالة جاهزة وغير قابلة للبلوغ)** — قراءة الحماية ترجع 403 بدل 404، فيستحيل إنتاج الـ409 المقصود بهذا التوكن | PR #3 جاهز (fixture) |
| 4c | Replay للموافقة | **PASS (مرفوضة كما يجب)** — إعادة استخدام approvalId مستهلك → 404 «الموافقة غير موجودة أو انتهت صلاحيتها» | `driver-results.json` (A3) + `ledger.json` |
| 4d | Allowlist (مستودع خارج القائمة) | **PASS (مرفوض كما يجب)** — `elazamey/not-in-allowlist` → 403 | `driver-results.json` (C1) + `ledger.json` |
| 5 | Evidence Bundle | **محفوظ** | `evidence/real-github-e2e/20260905T121636Z/` (9 ملفات، بدون أي توكن) |
| 6 | مراجعة Ledger + Evidence | **تمت** — 13 إدخالًا append-only يتطابق مع السلسلة: PLAN → APPROVAL → TOOL_RESULT → POLICY_CHECK → EXECUTION(BLOCKED) لكل محاولة، وERROR لكل رفض | `evidence/.../ledger.json` |
| 7 | إصدار الحكم | **BLOCKED** (انظر أعلاه) | هذا الملف |

**لم يحدث أي دمج** أثناء التنفيذ: `main` بقي على `6c024338...` (قبل = بعد)، وكل PRs الاختبارية ما زالت مفتوحة — أي أن البوابة فاشلة-إلى-المغلق (fail-closed) في كل الحالات.

## ما تم إثباته فعليًا على GitHub الحقيقي

1. **تدفق CI حقيقي**: workflow `e2e` (push + pull_request) شغّل check runs حقيقية على 3 رؤوس commit، بنتائج مضبوطة: الأخضر (PR #1، #3) والأحمر عبر hook `e2e-fail` (PR #2).
2. **بوابة الموافقة أحادية الاستخدام**: approvalId جديد لكل خطة؛ إعادة استخدامه → 404.
3. **رفض CI الفاشل**: خطة دمج لـ PR أحمر → 409 قبل أي عملية كتابة.
4. **فشل مغلق عند عدم القدرة على التحقق**: حتى مع CI أخضر كامل، الوكيل رفض الدمج (403) لأن قراءة حالة حماية الفرع مرفوضة — أي أن النظام **لا يدمج أبدًا ما لم يثبت التحقق**.
5. **Allowlist صارم**: أي مستودع خارج `GITHUB_ALLOWED_REPOSITORIES` → 403.
6. **سجل تدقيق قابل للتدقيق**: كل حدث (خطة/موافقة/فحص/تنفيذ/رفض) مرتبط بـ `planId` و`approvalId` و`actor` و`intent` و`repository` و`pullRequest` و`requestedAction`.

## المعطِّل الوحيد (محسوم بدقة)

بيئة التنفيذ الحالية تعمل بتوكن **GitHub App installation** يغطي `elazamey/github.ai2` فقط، وصلاحياته: Contents/Issues/Pull-Requests write — **بدون Administration**:

- لا يقرأ `GET /branches/main/protection` (403) — فلا يمكن **إثبات** وجود Required Check.
- لا يكتبه (`PUT .../protection` → 403، `POST /rulesets` → 403) — فلا يمكن **إعداد** `e2e` كـ Required Check.
- لا ينشئ مستودعات (`POST /user/repos` → 403) — فلا يمكن إنشاء مستودع اختبار مخصص جديد.
- لا يملك أي صلاحية على `elazamey/arabic-devops-e2e-test` (403 حتى على إنشاء label) — لذا سطر الأدلة القديم (PR #1 هناك) لا يمكن إكمالُه من هنا.

التفاصيل الخام: `evidence/real-github-e2e/20260905T121636Z/permissions-probe.json`.

> ملاحظة بيئة: خروج الشبكة يمر عبر TLS proxy (`E2B Proxy CA`)؛ نودج يتطلّب `NODE_EXTRA_CA_CERTS=/etc/ssl/certs/ca-certificates.crt` (أول تشغيل للحارس انتهى بـ `fetch failed` لهذا السبب، لا بسبب البوابة).

## ما تبقى لإكمال PASS (قائمة إعادة التشغيل)

**كل شيء جاهز مسبقًا** — لا كود جديد، لا fixtures جديدة:

1. منح صلاحية **Administration (read + write)** لتطبيق Arena على المستودع المستهدف — أو تنفيذ محلي بتوكن PAT يملك `repo`/admin على المستودع.
2. ضبط Required Check (بعد توفر الصلاحية):
   ```bash
   gh api repos/elazamey/github.ai2/branches/main/protection --method PUT --input - <<'EOF'
   {
     "required_status_checks": { "strict": false, "contexts": ["e2e"] },
     "enforce_admins": true
   }
   EOF
   ```
   (نفس الأمر يعمل على `elazamey/arabic-devops-e2e-test` لإكمال سطر الأدلة القديم: دمج PR #1 هناك).
3. إعادة التشغيل (من `arabic-devops-mvp`):
   ```bash
   NODE_EXTRA_CA_CERTS=/etc/ssl/certs/ca-certificates.crt \
   GITHUB_REPOSITORY=elazamey/github.ai2 \
   GITHUB_ALLOWED_REPOSITORIES=elazamey/github.ai2 \
   AGENT_URL=http://127.0.0.1:8787 \
   E2E_PR_NUMBER=1 E2E_FAILED_CI_PR=2 E2E_UNPROTECTED_PR=3 \
   E2E_UNAUTHORIZED_REPOSITORY=elazamey/not-in-allowlist \
   E2E_CONFIRM_MERGE=YES npm run test:real
   ```
4. المتوقع عند توفر الصلاحية: `PASS` + Squash Merge حقيقي لـPR #1 + Evidence Bundle يكتبه الحارس نفسه (`summary.json`/`ledger.json`) + الـ409 على unprotected.

## fixtures الجاهزة على `elazamey/github.ai2` (مستودع الجلسة — الوحيد القابل للكتابة بهذا التوكن)

| PR | الفرع | القاعدة | CI على الرأس | الغرض |
|----|-------|---------|--------------|-------|
| #1 | `e2e/positive` | `main` | أخضر (run×2) | Positive — الدمج Squash عبر الوكيل |
| #2 | `e2e/failing-ci` (commit: `e2e-fail`) | `main` | أحمر (run واحد) | رفض 409 عند فشل CI |
| #3 | `e2e/unprotected-ci` | `unprotected-base` (بدون حماية) | أخضر (run×2) | رفض 409 لعدم حماية الفرع |

أضيف `.github/workflows/e2e.yml` إلى `main` (commit `6c024338`) وهو workflow الاختبار نفسه (نفس محتوى مستودع الاختبار الأول حرفيًا).

## سبب استخدام `elazamey/github.ai2` بدل `arabic-devops-e2e-test`

التوكن لا يملك أي وصول للكتابة إلى مستودع الاختبار القديم ولا صلاحية إنشاء مستودع جديد، فنفّذت البنية على مستودع الجلسة نفسه مع أدنى أثر ممكن: ملف workflow + 3 ملفات fixture + 4 فروع اختبار + 3 PR مفتوحة — **لم يُمس أي إنتاج آخر، ولم يحدث أي دمج على main**. عند إكمال الصلاحيات يمكن إعادة التوجيه إلى `arabic-devops-e2e-test` نفسه (بإضافة workflow + protection + PR هناك) إن رغبت بإغلاق سطر الأدلة القديم بدل هذا المستودع.
